# Development: no Docker needed for the database.
# The app uses SQLite (via better-sqlite3) stored locally.
#
# Start the dev server with:
#   pnpm dev
#
# The database file is created automatically at:
#   gleaned.db  (in the project root, git-ignored)
